# EXOLNET-MACOS-TOOLBOX

<VirtualHost *:80>
    ServerName wildcard.localhost
    ServerAlias *.localhost

    RewriteEngine on
    RewriteCond %{HTTP_HOST} ^/(.+).localhost
    RewriteRule ^ https://%1.ssl.localhost%{REQUEST_URI} [R=301,QSA,END]
</VirtualHost>
